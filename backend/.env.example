# App
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Supabase (server side - keep SECRET in real env, this is just example placeholders)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service-role-placeholder

# Optional direct Postgres (if you need)
DATABASE_URL=postgres://user:password@localhost:5432/plearn_db

# Auth
JWT_SECRET=replace-with-jwt-secret

# CORS origins (comma separated)
CORS_ORIGINS=http://localhost:3000

# Optional
SENTRY_DSN=
REDIS_URL=
